# Mediloon Backend - Minimal Dependencies
# For space-constrained environments (OpenRouter handles LLM inference)

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database
aiosqlite>=0.19.0

# Environment
python-dotenv>=1.0.0

# HTTP client
httpx>=0.26.0

# Vector store (uses onnxruntime for embeddings, much lighter than torch)
chromadb>=0.4.22

# OpenAI client (for OpenRouter)
openai>=1.12.0

# Observability (MANDATORY for hackathon)
langfuse>=2.0.0
