# Mediloon Backend - Vercel Deployment
# This file is used by Vercel's Python runtime to install dependencies.

# Web framework
fastapi>=0.109.0
uvicorn>=0.27.0

# Database
aiosqlite>=0.19.0
pg8000>=1.30.0

# Environment
python-dotenv>=1.0.0

# HTTP client
httpx>=0.26.0

# Data ingestion (Excel seed data — lightweight, no pandas)
openpyxl>=3.1.0

# Validation (EmailStr in auth routes)
pydantic[email]>=2.0.0

# Vector store (cloud - no local ML packages!)
pinecone>=5.0.0

# Observability
langfuse>=2.0.0
PyJWT>=2.8.0
